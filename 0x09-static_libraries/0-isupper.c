#include <ctype.h>
#include "main.h"

/**
 * _isupper - Check if a character is in uppercase
 * @c: The character to check
 * Return: On success 1.
 * Else return 0.
*/
int _isupper(int c)
{
return (isupper(c) ? 1 : 0);
}
