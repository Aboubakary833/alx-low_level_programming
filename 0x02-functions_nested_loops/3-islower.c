#include <ctype.h>
#include "main.h"

/**
 * Check if a character is in lower case
 * @param int c - Character to check case
 * @return 1 or 0
*/
int _islower(int c)
{
return islower(c) ? (1) : (0);
}