#include <stdlib.h>
#include "main.h"

/**
 * _abs - return absolute value of c
 * @c: The integer
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int _abs(int c)
{
return abs(c);
}
