#include <string.h>

/**
 * _strncpy - Copy a string
 * @dest: Destination
 * @src: Source
 * @n: The size
 * Return: char *
*/
char *_strncpy(char *dest, char *src, int n)
{
return (strncpy(dest, src, n));
}
