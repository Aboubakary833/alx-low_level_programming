#include <stdio.h>

/**
 * main - Print arguments length
 * @argc: The arguments array size
 * @argv: The arguments array
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
printf("%d\n", argc - 1);
(void)argv;
return (0);
}
