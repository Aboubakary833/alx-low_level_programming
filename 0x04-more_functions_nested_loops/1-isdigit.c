#include <ctype.h>
#include "main.h"

/**
 * _isupper - Check if a character is a digit
 * @c: Element to check
 * Return: On success 1.
 * Else return 0.
*/
int _isdigit(int c)
{
return (isdigit(c) ? 1 : 0);
}
