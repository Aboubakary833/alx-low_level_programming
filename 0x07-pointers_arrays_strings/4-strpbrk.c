#include <string.h>

/**
 * _strpbrk - Search a set of char in a string
 * @s: The string
 * @accept: The set of char
 * Return: char
*/
char *_strpbrk(char *s, char *accept)
{
return (strpbrk(s, accept));
}
